const express = require('express');


const router = express.Router();

// router.post('/')
// router.patch('/')

module.exports = router;

// create a contract
// view contracts of user
// update contract --> can only update due date